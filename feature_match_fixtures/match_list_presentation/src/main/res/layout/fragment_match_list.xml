<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fixtures_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginBottom="8dp"
        tools:listitem="@layout/match_list_row_item"/>

    <include android:id="@+id/include_progress_bar"
        layout="@layout/progress_dialog"
        android:visibility="gone"/>

    <include android:id="@+id/include_no_list_items"
        layout="@layout/no_list_items"
        android:visibility="gone"/>

    <include android:id="@+id/include_no_internet"
        layout="@layout/no_internet"
        android:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>